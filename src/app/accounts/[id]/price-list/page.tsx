import Link from "next/link";import {notFound} from "next/navigation";import {BottomNav} from "@/components/bottom-nav";import {PrintButton} from "@/components/print-button";import {SmartBackLink} from "@/components/smart-back-link";import {getAccountSellingData,type AccountSellingRow} from "@/lib/account-selling";import type {AccountContainerPreference} from "@/lib/package-eligibility";import {createSupabaseServerClient} from "@/lib/supabase";
type Group={item:AccountSellingRow;rows:AccountSellingRow[]};
export default async function AccountPriceListPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{format?:string}>}){const {id}=await params;const {format="all"}=await searchParams;const db=await createSupabaseServerClient();const {data:account}=await db.from("accounts").select("id,name,town,postcode,container_preference").eq("id",id).single();if(!account)notFound();const selling=await getAccountSellingData(db,id,(account.container_preference||"any") as AccountContainerPreference);const grouped=new Map<string,Group>();for(const row of selling.rows){if(format!=="all"&&row.broadFormat!==format)continue;const group=grouped.get(row.productId)||{item:row,rows:[]};group.rows.push(row);grouped.set(row.productId,group);}const products=[...grouped.values()].sort((a,b)=>a.item.productName.localeCompare(b.item.productName));return <div className="min-h-screen bg-slate-50 pb-24 text-slate-950 print:bg-white print:pb-0"><header className="border-b border-slate-200 bg-white print:border-0"><div className="mx-auto max-w-5xl px-4 py-5 sm:px-6 print:max-w-none print:px-0"><SmartBackLink href={`/accounts/${id}`} className="text-sm font-medium text-slate-500 print:hidden">← {account.name}</SmartBackLink><h1 className="mt-2 text-3xl font-semibold tracking-tight">Current beer & price list</h1><p className="mt-2 text-sm text-slate-500">Prepared for {account.name} · {dateLabel(new Date())} · {selling.observedAt?`availability checked ${relativeTime(selling.observedAt)}`:"availability not loaded"}</p><div className="mt-4 flex flex-wrap gap-2 print:hidden">{["all","cask","keg","can"].map(f=><Link key={f} href={`/accounts/${id}/price-list?format=${f}`} className={`rounded-full px-4 py-2 text-sm font-semibold ring-1 ring-inset ${format===f?"bg-slate-950 text-white ring-slate-950":"bg-white ring-slate-200"}`}>{cap(f)}</Link>)}<PrintButton/></div></div></header><main className="mx-auto max-w-5xl px-4 py-5 sm:px-6 print:max-w-none print:px-0">{selling.lastRefreshError&&selling.rows.length?<p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-900 print:hidden">Showing the last known availability; the latest refresh failed.</p>:null}<div className="space-y-4">{products.map(({item,rows})=><section key={item.productId} className="break-inside-avoid rounded-2xl border border-slate-200 bg-white p-4"><div className="flex gap-4">{item.imageUrl?<img src={item.imageUrl} alt="" className="h-20 w-20 rounded-xl object-cover"/>:null}<div><h2 className="text-lg font-semibold">{item.productName}</h2>{item.abv!=null?<p className="text-sm text-slate-500">{item.abv}% ABV</p>:null}{item.description?<p className="mt-2 text-sm text-slate-600">{item.description}</p>:null}</div></div><div className="mt-4 border-t border-slate-100">{rows.map(row=><div key={row.variantId} className="grid grid-cols-[1fr_auto] gap-3 border-b border-slate-50 py-2.5 text-sm"><span>{row.packageLabel}</span><span className="font-semibold">{money(row.customerPrice??row.listPrice)}</span></div>)}</div></section>)}</div>{!products.length?<div className="rounded-2xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">{selling.observedAt?"No currently available products match this format and account’s package rules.":"Availability has not been loaded yet."}</div>:null}</main><BottomNav active="Accounts"/></div>}
function relativeTime(v:string){const m=Math.max(0,Math.round((Date.now()-new Date(v).getTime())/60000));return m<60?`${m}m ago`:`${Math.round(m/60)}h ago`}function money(v:number|null){return v==null?"—":new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP"}).format(v)}function cap(v:string){return v==="all"?"All":v[0].toUpperCase()+v.slice(1)}function dateLabel(d:Date){return new Intl.DateTimeFormat("en-GB",{day:"numeric",month:"long",year:"numeric"}).format(d)}
